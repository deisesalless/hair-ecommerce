spring.application.name=hair

# Configuracao no nivel do log
logging.level.br.com.store.hair=DEBUG

# Configuracao do PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/hair-store
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuracao endpoint health
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when_authorized

# Configuracao do JPA e Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false